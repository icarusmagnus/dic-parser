pdfplumber>=0.11    # extraction texte + tableaux des DIC PDF
openpyxl>=3.1       # lecture des EPT au format .xlsx
# camelot-py[cv]    # optionnel : extraction de tableaux difficiles (nécessite ghostscript)
